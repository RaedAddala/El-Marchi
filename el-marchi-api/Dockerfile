# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build el-marchi-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t el-marchi-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system el-marchi-api && \
          adduser --system -G el-marchi-api el-marchi-api

COPY dist/el-marchi-api el-marchi-api/
RUN chown -R el-marchi-api:el-marchi-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix el-marchi-api --omit=dev -f install

CMD [ "node", "el-marchi-api" ]
